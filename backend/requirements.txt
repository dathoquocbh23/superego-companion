fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
google-genai>=1.0
anthropic>=0.40          # tuỳ chọn — chỉ cần khi LLM_PROVIDER=anthropic
redis>=5.2
networkx>=3.4
pyyaml>=6.0
python-dotenv>=1.0
sse-starlette>=2.1

# dev / test
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27

# giai đoạn 2 (phân tích log) — chưa cài ở demo
# pandas
# scipy
# pingouin
